# Source file inventory

## Included as direct reference

- `references/source-decks/01-low-density-static-reference.pdf`
  - First uploaded deck.
  - Use only as visual cleanliness reference.
  - Problem: too static, too low-density, too many steps collapsed into one page.

- `references/source-decks/02-content-heavy-draft-reference.pdf`
  - Second uploaded deck.
  - Use as content source.
  - Problem: draft notes, inconsistent density, inconsistent layout.

## Included as cleaned assets

Assets from `발표_리소스` and `working.zip` were renamed into ASCII paths under `input/assets`.

## Included as code excerpts

Important source code excerpts from `smartdrain-dev` were copied to `references/code-excerpts`.
They are for claim checking and technical accuracy, not for running the app.

## Excluded from starter package

- Full `smartdrain-dev` repository zip: too large and unnecessary for slide generation.
- Full ExportBlock archive: many files are unrelated or raw Notion exports.
- FigJam mega image: too large and not directly useful as a slide asset.
- YOLO model `.pt` file: not needed for slide generation.
- Unrelated auth/user API CSVs from old export data.

## Asset use caution

- The messy YOLO/OpenCV process screenshot should be recreated as a clean diagram.
- ERD and sequence images can be used as reference, but if text is too small, recreate them.
- News screenshot/reference image should not be used in final unless source handling is confirmed.
