# 작업 프롬프트

이 저장소에서 발표자료를 점검하거나 수정할 때는 아래 규칙을 따른다.

## 절대 기준

- `output/presentation.html`만 정본으로 본다.
- 스킬은 사용하지 않는다.
- 이전 문서, PDF, QA 이미지, 스크린샷, 기타 산출물은 수정 전 기준으로 만들어졌을 수 있으므로 현재 구조 판단의 기준으로 삼지 않는다.
- 페이지 수나 `data-page` 표기 불일치는 사용자가 직접 수정하면서 생긴 오차일 수 있다.
- 현재 `presentation.html`의 내용과 HTML에 배치된 슬라이드 순서가 옳은 기준이다.

## 현재 작업 기준

- 실제 HTML 슬라이드 수는 30장이다.
- 내용 순서는 현재 `presentation.html`의 실제 섹션 순서를 유지한다.
- 표시용 `data-page`와 초기 카운터는 실제 30장 기준으로 `01 / 30`부터 `30 / 30`까지 차례대로 맞춘다.
- 목차는 현재 실제 슬라이드 구성과 맞는지 함께 점검하고, 불일치한 문구만 최소 수정한다.
- 수정 전 기준의 HTML 순서는 다음과 같다.

```text
01, 02, 03, 04, 05, 06, 07, 08, 09, 10,
11, 12, 13, 14, 15, 16, 18, 19, 20, 27,
21, 22, 23, 24, 25, 26, 31, 32, 33, 34
```

- 빠진 표시 번호는 `17`, `28`, `29`, `30`이다.
- `20 / 34` 다음에 `27 / 34`가 먼저 나오고, 이후 `21 / 34`부터 `26 / 34`가 이어진다.
- 위 표기 오차는 정리하되, 슬라이드 내용 순서 자체는 유지한다.

## 작업 방식

- 수정 전에는 먼저 커밋 상태를 확인한다.
- 사용자가 만든 변경을 되돌리지 않는다.
- 필요한 경우 현재 상태를 먼저 커밋한 뒤 작업한다.
- 분석은 `output/presentation.html`의 실제 HTML 구조, 섹션 순서, 참조 에셋 기준으로 진행한다.
- 슬라이드 순서를 임의로 재정렬하지 않는다.
- 현재 명시 요청에 따라 페이지 표기는 30장 기준으로 정리한다.

## Git 메모

- 현재 발표자료 스냅샷 커밋은 원격 `main`에 반영되어 있다.
- push 거절이 발생하면 `git fetch origin` 후 `git rebase origin/main`으로 원격 변경을 통합하고 다시 push한다.
